naftiko: 1.0.0-alpha2
info:
  label: Couchbase Sync Gateway Admin REST API — Replication
  description: 'Couchbase Sync Gateway Admin REST API — Replication. 5 operations. Lead operation: List replications. Self-contained
    Naftiko capability covering one Couchbase business surface.'
  tags:
  - Couchbase
  - Replication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    COUCHBASE_API_KEY: COUCHBASE_API_KEY
capability:
  consumes:
  - type: http
    namespace: sync-gateway-admin-rest-replication
    baseUri: https://localhost:4985
    description: Couchbase Sync Gateway Admin REST API — Replication business capability. Self-contained, no shared references.
    resources:
    - name: db-_replication
      path: /{db}/_replication/
      operations:
      - name: listreplications
        method: GET
        description: List replications
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createreplication
        method: POST
        description: Create a replication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: db-_replication-replicationId
      path: /{db}/_replication/{replicationId}
      operations:
      - name: getreplication
        method: GET
        description: Get replication details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatereplication
        method: PUT
        description: Update a replication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletereplication
        method: DELETE
        description: Delete a replication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: basic
      username: '{{env.COUCHBASE_USER}}'
      password: '{{env.COUCHBASE_PASS}}'
  exposes:
  - type: rest
    namespace: sync-gateway-admin-rest-replication-rest
    port: 8080
    description: REST adapter for Couchbase Sync Gateway Admin REST API — Replication. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/{db}/replication
      name: db-replication
      description: REST surface for db-_replication.
      operations:
      - method: GET
        name: listreplications
        description: List replications
        call: sync-gateway-admin-rest-replication.listreplications
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createreplication
        description: Create a replication
        call: sync-gateway-admin-rest-replication.createreplication
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/{db}/replication/{replicationid}
      name: db-replication-replicationid
      description: REST surface for db-_replication-replicationId.
      operations:
      - method: GET
        name: getreplication
        description: Get replication details
        call: sync-gateway-admin-rest-replication.getreplication
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatereplication
        description: Update a replication
        call: sync-gateway-admin-rest-replication.updatereplication
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletereplication
        description: Delete a replication
        call: sync-gateway-admin-rest-replication.deletereplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sync-gateway-admin-rest-replication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Couchbase Sync Gateway Admin REST API — Replication. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-replications
      description: List replications
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: sync-gateway-admin-rest-replication.listreplications
      outputParameters:
      - type: object
        mapping: $.
    - name: create-replication
      description: Create a replication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: sync-gateway-admin-rest-replication.createreplication
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-replication-details
      description: Get replication details
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: sync-gateway-admin-rest-replication.getreplication
      outputParameters:
      - type: object
        mapping: $.
    - name: update-replication
      description: Update a replication
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: sync-gateway-admin-rest-replication.updatereplication
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-replication
      description: Delete a replication
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: sync-gateway-admin-rest-replication.deletereplication
      outputParameters:
      - type: object
        mapping: $.
